pipeline {
    agent any

    stages {

        stage('Clone Repository') {
            steps {
                git 'https://github.com/Surya3112-cloud/car-website.git'
            }
        }

        stage('Build') {
            steps {
                echo "Car Website Pipeline Running"
            }
        }

    }
}
